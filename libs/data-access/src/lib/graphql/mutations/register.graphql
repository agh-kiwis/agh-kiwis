mutation Register($registerDto: AuthEmailRegisterInput!) {
  register(registerDto: $registerDto) {
    birthDate
    createdAt
    deletedAt
    email
    gender
    id
    name
    updatedAt
    token
  }
}
